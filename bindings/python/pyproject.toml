[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "matchingengine"
version = "0.1.0"
description = "Python bindings for MatchingEngine (ctypes + MessagePack)"
requires-python = ">=3.9"
dependencies = ["msgpack>=1.0"]

